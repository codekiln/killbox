{
  "name": "killbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "lint": "tsc --noEmit --pretty false",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npm run test:e2e:local",
    "test:e2e:local": "npm run build && playwright test",
    "test:e2e:public": "playwright test",
    "spec-check": "openspec validate --all --strict --no-interactive",
    "check": "npm run lint && npm run test && npm run build && npm run spec-check"
  },
  "dependencies": {
    "phaser": "latest"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "@types/node": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
